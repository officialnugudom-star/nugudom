# Firebase
.firebase/
firebase-debug.log
# .firebaserc — keep this, it only has the project ID

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.local

# Large media. 310 MB of MP4s as of this writing - too big for git.
# TODO: move to Firebase Storage with public URLs in the HTML so the
# GitHub Actions deploy stays self-contained. Until then, new videos
# need a manual `firebase deploy` to refresh on live.
public/videos/
